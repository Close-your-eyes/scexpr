% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cell_segmentation_spatial.R
\name{get_mask_subset}
\alias{get_mask_subset}
\title{Subset Spatial Mask to Region of Interest}
\usage{
get_mask_subset(
  obj,
  mask,
  cell_coords = NULL,
  hull_filter = NULL,
  subset_factor = 1
)
}
\arguments{
\item{obj}{seurat object}

\item{mask}{A raw image or mask array to convert and subset.}

\item{cell_coords}{Optional matrix or data frame of cell coordinates. If
provided and \code{hull_filter} is \code{NULL}, a convex hull is computed from these
coordinates to define the region of interest.}

\item{hull_filter}{Optional data frame with columns \code{x}, \code{y} defining a polygon
used to filter the mask. Overrides \code{cell_coords} if provided.}

\item{subset_factor}{Numeric. Downsampling factor for the mask to improve
performance. Default is \code{1} (no downsampling).}
}
\value{
A data frame containing at least \code{x}, \code{y}, and optionally \code{z} and
\code{color} columns, depending on the input mask.
}
\description{
Converts a raw image or mask array into a data frame and optionally restricts
it to a region defined by cell coordinates or a custom polygon.
}
\details{
The function performs the following steps:
\itemize{
\item Optionally computes a convex hull from \code{cell_coords}
\item Converts the mask array into a data frame using
\code{raw_img_array_to_df()}
\item Filters pixels inside the hull (if provided)
\item Optionally filters by cell IDs (\code{z}) matching \code{cell_coords}
}

If \code{cell_coords} has more than two columns, only the first two are used.
}
