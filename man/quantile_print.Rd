% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quantile_print.R
\name{quantile_print}
\alias{quantile_print}
\title{Summarize Quantiles of a Seurat Metadata Variable}
\usage{
quantile_print(
  obj,
  meta_col,
  probs = seq(0.1, 1, 0.1),
  decimals = NULL,
  names = c("probs_cut", "probs", "pct"),
  kable = TRUE
)
}
\arguments{
\item{obj}{A Seurat object.}

\item{meta_col}{A single character string naming a numeric column in
\code{obj@meta.data}.}

\item{probs}{A numeric vector of probabilities between zero and one.
Defaults to deciles from \code{0.1} through \code{1}.}

\item{decimals}{An optional non-negative integer specifying the number of
decimal places to which quantile values are rounded. When \code{NULL}, values
are not rounded.}

\item{names}{Character string controlling the quantile labels. Must be one
of \code{"probs_cut"}, \code{"probs"}, or \code{"pct"}. \code{"probs_cut"} removes the leading
zero from probabilities, \code{"probs"} uses the probabilities directly, and
\code{"pct"} retains the percentage labels generated by \code{\link[stats:quantile]{stats::quantile()}}.}

\item{kable}{Logical. If \code{TRUE}, return a table formatted with
\code{\link[knitr:kable]{knitr::kable()}}. If \code{FALSE}, return a tibble.}
}
\value{
A \code{knitr_kable} object when \code{kable = TRUE}; otherwise, a tibble with
columns \code{q} (quantile label) and the name supplied through \code{meta_col}
(quantile value).
}
\description{
Calculates selected quantiles for a numeric column in a Seurat object's
metadata. The result can be returned as a tibble or formatted as a
\code{knitr::kable()} table.
}
\examples{
\dontrun{
quantile_print(
  obj = seurat_object,
  meta_col = "nCount_RNA"
)

quantile_print(
  obj = seurat_object,
  meta_col = "percent.mt",
  probs = c(0.25, 0.5, 0.75),
  decimals = 2,
  names = "pct",
  kable = FALSE
)
}
}
