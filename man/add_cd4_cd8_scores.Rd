% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/derive_cd4_cd8_tcell_lineage.R
\name{add_cd4_cd8_scores}
\alias{add_cd4_cd8_scores}
\title{Add CD4 and CD8 Gene-Signature Scores}
\usage{
add_cd4_cd8_scores(obj, label_col = "cd4cd8lin", ncores = 8)
}
\arguments{
\item{obj}{A Seurat object containing normalized expression data and a
cell-level metadata column with CD4/CD8 lineage labels.}

\item{label_col}{Character scalar giving the name of the metadata column
containing the labels \code{"CD4"} and \code{"CD8"}. Defaults to \code{"cd4cd8lin"}.}

\item{ncores}{Positive integer specifying the number of CPU cores passed to
\code{\link[UCell:AddModuleScore_UCell]{UCell::AddModuleScore_UCell()}}. Defaults to \code{8}.}
}
\value{
The input Seurat object with \code{CD4score_UCell}, \code{CD8score_UCell},
and \code{diff_CD4_CD8_score} added to its cell-level metadata.
}
\description{
Identifies marker genes distinguishing CD4 and CD8 cells and calculates
UCell module scores for each signature. Marker genes are restricted to
features detected in more than 30 percent of cells with an adjusted
p-value below \code{1e-5}. Up to 20 top markers per group are selected using
both \code{avg_log2FC} and \code{logFC}.
}
\details{
The canonical marker genes \code{CD4}, \code{CD8A}, and \code{CD8B} are excluded from
their corresponding signatures. The resulting CD4 score, CD8 score, and
their difference are stored in the object's cell-level metadata as
\code{CD4score_UCell}, \code{CD8score_UCell}, and \code{diff_CD4_CD8_score}, respectively.
}
\examples{
\dontrun{
object <- add_cd4_cd8_scores(
  object,
  label_col = "cd4cd8lin",
  ncores = 4
)

head(object[[]][
  c("CD4score_UCell", "CD8score_UCell", "diff_CD4_CD8_score")
])
}
}
