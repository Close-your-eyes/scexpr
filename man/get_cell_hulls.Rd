% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cell_segmentation_spatial.R
\name{get_cell_hulls}
\alias{get_cell_hulls}
\title{Compute Cell Hull Polygons from Spatial Mask}
\usage{
get_cell_hulls(
  obj,
  mask = obj@misc[["spatial"]][["staining_image_mask"]],
  cell_coords = obj@reductions$spatial@cell.embeddings,
  hull_filter = NULL
)
}
\arguments{
\item{obj}{A spatial object containing image and coordinate data (e.g.,
Seurat-like object with \verb{@misc[["spatial"]]} and \verb{@reductions$spatial}).}

\item{mask}{A mask array representing segmented regions. Defaults to
\code{obj@misc[["spatial"]][["staining_image_mask"]]}.}

\item{cell_coords}{A matrix or data frame of cell coordinates (typically
\code{obj@reductions$spatial@cell.embeddings}). Used for filtering and defining
the region of interest.}

\item{hull_filter}{Optional data frame of coordinates (\code{x}, \code{y}) defining a
polygon to restrict the mask. If \code{NULL}, a convex hull is computed from
\code{cell_coords}.}
}
\value{
A data frame with columns \code{x}, \code{y}, and \code{z}, representing polygon
vertices for each cell hull.
}
\description{
Extracts convex hull polygons for each cell (grouped by \code{z}) from a spatial
mask. The mask is optionally filtered to a region defined by cell coordinates
or a custom hull.
}
\details{
This function:
\itemize{
\item Subsets the mask using \code{get_mask_subset()}
\item Groups pixels by cell ID (\code{z})
\item Computes a convex hull per group using \code{grDevices::chull()}
}

The resulting output can be used for plotting segmentation boundaries.
}
