% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/raw_img_array_to_df.R
\name{raw_img_array_to_df}
\alias{raw_img_array_to_df}
\title{Convert Image Array to Data Frame for Plotting}
\usage{
raw_img_array_to_df(
  raw_array,
  subset_factor = 1,
  hull_filter = NULL,
  hull_inside = T,
  xrange = NULL,
  yrange = NULL
)
}
\arguments{
\item{raw_array}{A numeric or integer array representing an image.
Must be either:
\itemize{
\item A 3D array with RGB channels in the third dimension
\item A 2D array representing a single-channel mask (e.g., segmentation IDs)
}}

\item{subset_factor}{Numeric. Downsampling factor for rows and columns to
reduce memory usage and improve performance. Default is \code{1} (no downsampling).}

\item{hull_filter}{Optional data frame with columns \code{x} and \code{y} defining a
polygon. Used to subset the output to points inside (or outside) the polygon.}

\item{hull_inside}{Logical. If \code{TRUE} (default), keeps only points inside or on
the polygon defined by \code{hull_filter}. If \code{FALSE}, keeps only points outside.}

\item{xrange}{Optional numeric vector of length 2 specifying the x-range to
retain (min, max).}

\item{yrange}{Optional numeric vector of length 2 specifying the y-range to
retain (min, max).}
}
\value{
A data frame with:
\itemize{
\item \code{x}, \code{y}: pixel coordinates
\item \code{color}: hex color values (for RGB input), OR
\item \code{z}: pixel values (for 2D input, returned as character)
}
}
\description{
Converts a 2D or 3D image array into a data frame with \code{x}/\code{y} pixel
coordinates, suitable for visualization with \code{ggplot2}. Supports RGB
images (3D arrays) as well as single-channel masks (2D arrays).
}
\details{
The function performs the following steps:
\itemize{
\item Converts the input array to integer values
\item Subsamples pixels using \code{subset_factor}
\item Expands pixel indices into \code{x}, \code{y} coordinate pairs
\item Extracts RGB channels (for 3D arrays) or assigns values to \code{z} (for 2D arrays)
\item Optionally filters points using:
\itemize{
\item Bounding box filters (\code{xrange}, \code{yrange})
\item Polygon inclusion via \code{sp::point.in.polygon()}
}
\item For RGB images, combines channels into a hex color string using
\code{grDevices::rgb()}
}

Filtering is applied after subsampling to improve efficiency.
}
