% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reverse_lognorm.R
\name{sacrifice_count_slot}
\alias{sacrifice_count_slot}
\title{Remove the RNA counts layer while preserving cell totals}
\usage{
sacrifice_count_slot(obj, rm_count = F)
}
\arguments{
\item{obj}{A Seurat object containing an RNA assay with a \code{"counts"} layer.}

\item{rm_count}{remove the count layer (if F: just RNA_count_colSums added)}
}
\value{
A Seurat object with the RNA counts layer removed and its per-cell
column sums stored in \code{Seurat::Misc()}.
}
\description{
Removes the \code{"counts"} layer from the RNA assay to reduce the size of a
Seurat object. Before removal, the total count for each cell is stored in
the object's miscellaneous data under \code{"RNA_count_colSums"}.
}
\details{
The stored column sums can be used by \code{\link[=reverse_lognorm]{reverse_lognorm()}} to approximate
the original counts from the log-normalized data layer.
}
\examples{
\dontrun{
obj <- sacrifice_count_slot(obj)

# Reconstruct the counts matrix
counts <- reverse_lognorm(obj)
}
}
