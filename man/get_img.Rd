% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cell_segmentation_spatial.R
\name{get_img}
\alias{get_img}
\title{Extract and Prepare Spatial Image Data for Plotting}
\usage{
get_img(
  obj,
  img_array = obj@misc[["spatial"]][["staining_image"]],
  cell_coords = obj@reductions$spatial@cell.embeddings,
  hull_filter = NULL,
  subset_factor = 1,
  col_conv_fun = NULL
)
}
\arguments{
\item{obj}{seurat object}

\item{img_array}{A raw image array. Defaults to
\code{obj@misc[["spatial"]][["staining_image"]]}.}

\item{cell_coords}{Optional matrix or data frame of cell coordinates used to
define a region of interest.}

\item{hull_filter}{Optional polygon (\code{x}, \code{y}) used to restrict the image.}

\item{subset_factor}{Numeric. Downsampling factor for performance. Default is \code{1}.}

\item{col_conv_fun}{Optional function to transform color values (e.g.,
grayscale conversion).}
}
\value{
A data frame with columns \code{x}, \code{y}, and \code{color}.
}
\description{
Converts a spatial image array into a data frame suitable for plotting,
optionally restricting it to a region of interest and applying a color
transformation.
}
\details{
This function:
\itemize{
\item Calls \code{get_mask_subset()} to subset and reshape the image
\item Optionally transforms the \code{color} column using \code{col_conv_fun}
}

The output is typically used with \code{ggplot2::geom_raster()}.
}
