% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/FindNeighbors2.R
\name{FindNeighbors2}
\alias{FindNeighbors2}
\title{Find nearest-neighbor and shared-nearest-neighbor graphs}
\usage{
FindNeighbors2(
  object,
  query = NULL,
  distance.matrix = FALSE,
  k.param = 20,
  return.neighbor = FALSE,
  compute.SNN = !return.neighbor,
  prune.SNN = 1/15,
  nn.method = "annoy",
  n.trees = 50,
  annoy.metric = "euclidean",
  nn.eps = 0,
  verbose = TRUE,
  l2.norm = FALSE,
  cache.index = FALSE,
  index = NULL,
  ...
)
}
\arguments{
\item{object}{A matrix-like object with cells in rows and features in columns,
or a precomputed distance matrix if \code{distance.matrix = TRUE}. Row names are
required and are used as cell names.}

\item{query}{Optional query matrix. If \code{NULL}, \code{object} is used as the query.}

\item{distance.matrix}{Logical. Whether \code{object} is a precomputed distance
matrix. Default is \code{FALSE}.}

\item{k.param}{Integer. Number of nearest neighbors to compute. Default is \code{20}.}

\item{return.neighbor}{Logical. Whether to return the nearest-neighbor object
directly instead of graph objects. Default is \code{FALSE}.}

\item{compute.SNN}{Logical. Whether to compute the SNN graph. Defaults to
\code{!return.neighbor}.}

\item{prune.SNN}{Numeric. SNN pruning threshold. Edges with values less than
or equal to this threshold are removed. Default is \code{1/15}.}

\item{nn.method}{Character. Nearest-neighbor search method. Default is
\code{"annoy"}.}

\item{n.trees}{Integer. Number of trees to use when \code{nn.method = "annoy"}.
Default is \code{50}.}

\item{annoy.metric}{Character. Distance metric used by Annoy. Default is
\code{"euclidean"}.}

\item{nn.eps}{Numeric. Error bound for nearest-neighbor search. Default is \code{0}.}

\item{verbose}{Logical. Whether to print progress messages. Default is \code{TRUE}.}

\item{l2.norm}{Logical. Whether to L2-normalize \code{object} and \code{query} before
neighbor search. Default is \code{FALSE}.}

\item{cache.index}{Logical. Whether to cache the Annoy index. Default is
\code{FALSE}.}

\item{index}{Optional precomputed nearest-neighbor index.}

\item{...}{Additional arguments. Currently checked by \code{Seurat:::CheckDots}.}
}
\value{
A list with two elements:
\describe{
\item{graphs}{A list containing the KNN graph as \code{nn} and, if requested,
the SNN graph as \code{snn}.}
\item{nn.ranked}{The nearest-neighbor object returned by Seurat's internal
neighbor search helper.}
}

If \code{return.neighbor = TRUE}, the nearest-neighbor object is returned directly
and graph construction is skipped.
}
\description{
Computes a k-nearest-neighbor (KNN) graph, and optionally a
shared-nearest-neighbor (SNN) graph, from an input matrix or distance matrix.
This is a modified neighbor-finding helper based on Seurat internals.
It always returns the ranked nearest neighbor graph.
}
\examples{
\dontrun{
mat <- matrix(rnorm(1000), nrow = 100)
rownames(mat) <- paste0("cell_", seq_len(nrow(mat)))

neighbors <- FindNeighbors2(mat, k.param = 20)
names(neighbors$graphs)
}
}
