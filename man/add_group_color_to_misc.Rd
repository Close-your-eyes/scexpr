% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_group_color_to_misc.R
\name{add_group_color_to_misc}
\alias{add_group_color_to_misc}
\title{Add meta_col-specific colors to a Seurat object's misc slot}
\usage{
add_group_color_to_misc(
  obj,
  meta_col = "orig.ident",
  colors = colrr::col_pal("custom", return = "c")
)
}
\arguments{
\item{obj}{A Seurat object containing a \code{meta.data} slot.}

\item{meta_col}{Character scalar. Name of the metadata column used to define groups.
Defaults to \code{"orig.ident"}.}

\item{colors}{A character vector of colors. Can be named (matching meta_col levels)
or unnamed. If unnamed or partially named, colors are assigned in order to
sorted unique meta_col values.
Defaults to \code{colrr::col_pal("custom")}.}
}
\value{
A modified Seurat object.
}
\description{
Assigns a named vector of colors to groups defined in a metadata column
and stores the result in the \code{misc} slot of a Seurat object.
}
\details{
If \code{colors} is a named vector and all names match the meta_col levels,
it is used directly. Otherwise, colors are assigned sequentially to the
sorted unique values of the grouping variable.
}
\examples{
\dontrun{
so <- add_group_color_to_misc(so, "celltype")
}
}
