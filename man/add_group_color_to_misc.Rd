% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_group_color_to_misc.R
\name{add_group_color_to_misc}
\alias{add_group_color_to_misc}
\title{Add group-specific colors to a Seurat object's misc slot}
\usage{
add_group_color_to_misc(
  obj,
  group = "orig.ident",
  colors = colrr::col_pal("custom"),
  name = paste0(group, "_colors")
)
}
\arguments{
\item{obj}{A Seurat object containing a \code{meta.data} slot.}

\item{group}{Character scalar. Name of the metadata column used to define groups.
Defaults to \code{"orig.ident"}.}

\item{colors}{A character vector of colors. Can be named (matching group levels)
or unnamed. If unnamed or partially named, colors are assigned in order to
sorted unique group values.
Defaults to \code{colrr::col_pal("custom")}.}

\item{name}{Character scalar. Name under which the color vector will be stored
in \code{obj@misc}. Defaults to \code{paste0(group, "_colors")}.}
}
\value{
A modified Seurat object with a named color vector stored in
\code{obj@misc[[name]]}.
}
\description{
Assigns a named vector of colors to groups defined in a metadata column
and stores the result in the \code{misc} slot of a Seurat object.
}
\details{
If \code{colors} is a named vector and all names match the group levels,
it is used directly. Otherwise, colors are assigned sequentially to the
sorted unique values of the grouping variable.
}
