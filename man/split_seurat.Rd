% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/split_seurat.R
\name{split_seurat}
\alias{split_seurat}
\title{Split a Seurat Object into Cell Chunks}
\usage{
split_seurat(
  obj,
  chunks = 2,
  size = NULL,
  shuffle = F,
  diet = F,
  diet_args = list(misc = F, layers = "counts", assays = "RNA")
)
}
\arguments{
\item{obj}{A Seurat object.}

\item{chunks}{Integer. Number of chunks to create. Defaults to \code{2}.}

\item{size}{Optional integer specifying the desired number of cells per
chunk. Passed to \code{\link[brathering:split_chunks]{brathering::split_chunks()}}.}

\item{shuffle}{Logical. Whether to randomly shuffle cells before splitting.
Defaults to \code{FALSE}.}

\item{diet}{Logical. Whether to apply \code{\link[Seurat:DietSeurat]{Seurat::DietSeurat()}} before
splitting. Defaults to \code{FALSE}.}

\item{diet_args}{Named list of additional arguments passed to
\code{\link[Seurat:DietSeurat]{Seurat::DietSeurat()}}. The input object is supplied automatically as
\code{object}. Defaults to retaining the RNA counts layer and dropping \code{misc}.}
}
\value{
A list of Seurat objects, with one object for each cell chunk.
}
\description{
Splits the cells of a Seurat object into chunks and returns one subsetted
Seurat object per chunk. Optionally reduces the object with
\code{\link[Seurat:DietSeurat]{Seurat::DietSeurat()}} before splitting.
}
\examples{
\dontrun{
chunks <- split_seurat(pbmc_small, chunks = 2)

chunks <- split_seurat(
  pbmc_small,
  chunks = 3,
  shuffle = TRUE,
  diet = TRUE
)
}
}
