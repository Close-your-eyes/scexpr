% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/image_feature_plot.R
\name{image_feature_plot}
\alias{image_feature_plot}
\title{Plot Spatial Image with Optional Feature Overlay}
\usage{
image_feature_plot(
  obj,
  feature = NULL,
  plot_img = T,
  img_subset_fct = 4,
  img_col_conv_fun = colrr::hex_to_grey,
  plot_cell_seg = T,
  hull_df = NULL,
  cell_seg_args = list(color = NA, linewidth = 0.1, alpha = 0.7),
  cell_point_args = list(size = 1, alpha = 0.1),
  get_data_args = list(layer = "data"),
  axes_hide = T,
  col_pal_c_args = list(name = "spectral", direction = -1),
  col_pal_d_args = list(name = "custom"),
  col_binary = F
)
}
\arguments{
\item{obj}{seurat object}

\item{feature}{Character scalar. Name of the feature to visualize. If \code{NULL},
only structural elements (image or segmentation) are shown.}

\item{plot_img}{Logical. Whether to render the background image. Default is \code{TRUE}.}

\item{img_subset_fct}{Numeric. Downsampling factor for the image to improve
plotting performance. Default is \code{4}.}

\item{img_col_conv_fun}{Function. Function used to convert image colors
(e.g., \code{colrr::hex_to_grey}). Default is \code{colrr::hex_to_grey}.}

\item{plot_cell_seg}{Logical. Whether to plot cell segmentation polygons.
If \code{FALSE}, cell-level points are used instead. Default is \code{TRUE}.}

\item{hull_df}{provide data frame of cell hulls directly}

\item{cell_seg_args}{List. Additional arguments passed to
\code{ggplot2::geom_polygon()} for segmentation plotting (e.g., \code{color},
\code{linewidth}, \code{alpha}).}

\item{cell_point_args}{List. Additional arguments passed to
\code{ggplot2::geom_point()} for point-based plotting (e.g., \code{size}, \code{alpha}).}

\item{get_data_args}{List. Additional arguments passed to the internal
\code{get_data()} function (e.g., \code{layer = "data"}).}

\item{axes_hide}{Logical. Whether to hide axis labels, ticks, and text.
Default is \code{TRUE}.}

\item{col_pal_c_args}{List. Arguments for continuous color palette generation
using \code{colrr::make_col_pal()}. Must include \code{name}.}

\item{col_pal_d_args}{List. Arguments for discrete color palette generation
using \code{colrr::make_col_pal()}. Must include \code{name}.}
}
\value{
A \code{ggplot2} object representing the spatial visualization.
}
\description{
Generates a ggplot2-based visualization of spatial imaging data, optionally
overlaying cell segmentation polygons or cell-level points colored by a
selected feature. Supports background image rendering and flexible styling.
}
\details{
The function supports three main visualization modes:
\itemize{
\item Background image only
\item Cell segmentation polygons (optionally colored by feature)
\item Cell-level points (if segmentation is disabled)
}

When a feature is provided:
\itemize{
\item Numeric features are visualized using a continuous color scale
\item Categorical features are converted to factors and visualized using
a discrete palette
}

Multiple features are currently not supported.
}
\examples{
\dontrun{
# Basic usage with segmentation
p <- image_feature_plot(obj, feature = "GeneA")
print(p)

# Plot without image background
p <- image_feature_plot(obj, feature = "GeneA", plot_img = FALSE)

# Plot using points instead of segmentation
p <- image_feature_plot(obj, feature = "GeneA", plot_cell_seg = FALSE)

# Customize segmentation appearance
p <- image_feature_plot(
  obj,
  feature = "GeneA",
  cell_seg_args = list(color = "black", linewidth = 0.2, alpha = 0.8)
)

# Use custom color palette
p <- image_feature_plot(
  obj,
  feature = "GeneA",
  col_pal_c_args = list(name = "viridis")
)
}
}
