% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/freq_expression.R
\name{freq_expression}
\alias{freq_expression}
\title{Calculate the fraction of cells expressing one or more features}
\usage{
freq_expression(
  obj,
  features,
  meta_col,
  assay = "RNA",
  cells = NULL,
  split_feature = NULL,
  format_pct = F
)
}
\arguments{
\item{obj}{A Seurat object or list of Seurat objects.}

\item{features}{Character vector of feature (gene) names whose expression
frequency should be calculated.}

\item{meta_col}{Metadata column used to define the groups for which
expression frequencies are computed.}

\item{assay}{Assay from which expression values are extracted.}

\item{cells}{Optional vector of cell names to subset prior to computing
expression frequencies. If \code{NULL}, all cells are used.}

\item{split_feature}{Optional metadata column used to split the data before
summarization. Frequencies are calculated independently for each level of
this variable.}

\item{format_pct}{Logical. If \code{TRUE}, add formatted character columns for the
expression frequencies (e.g. \code{"25 \%"} and \code{"0.25"}).}
}
\value{
A data frame containing one row per feature (or feature split) and
group. The output includes:
\describe{
\item{feature_split}{Feature identifier (or feature/split combination).}
\item{meta_col}{Grouping variable supplied by \code{meta_col}.}
\item{SO.split}{Identifier of the originating Seurat object when multiple
objects are supplied.}
\item{max.feat.expr}{Maximum observed expression of the feature within the
subset.}
\item{pct.expr}{Fraction of cells with expression greater than zero.}
\item{pct.expr.adjust.pct}{Formatted percentage (only when
\code{format_pct = TRUE}).}
\item{pct.expr.adjust}{Formatted fraction (only when
\code{format_pct = TRUE}).}
}
}
\description{
Computes the fraction of cells with non-zero expression for one or more
features across groups defined by a metadata column. Optionally, cells can be
further split by an additional metadata variable before summarization.
}
\details{
Expression is considered positive when the extracted expression value is
greater than zero. Data are retrieved using \code{get_data()} with
\code{layer = "data"}.
}
\examples{
\dontrun{
## Fraction of cells expressing selected genes by cell type
freq_expression(
  obj = pbmc,
  features = c("MS4A1", "CD3D", "LYZ"),
  meta_col = "celltype"
)

## Split by sample
freq_expression(
  obj = pbmc,
  features = "MS4A1",
  meta_col = "celltype",
  split_feature = "sample"
)
}
}
