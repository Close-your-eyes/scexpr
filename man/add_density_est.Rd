% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_density_est.R
\name{add_density_est}
\alias{add_density_est}
\title{Add density estimates to cell metadata}
\usage{
add_density_est(
  obj,
  reduction,
  name = paste0(reduction, "_dens"),
  density_est_args = list(n = 200, h = 1),
  type = c("scaled", "raw")
)
}
\arguments{
\item{obj}{A Seurat object.}

\item{reduction}{Character. Name of the dimensional reduction to use.}

\item{name}{Character. Name of the metadata column to add. Defaults to
\code{paste0(reduction, "_dens")}.}

\item{density_est_args}{List of arguments passed to
\code{brathering::density_est()}. Defaults to \code{list(n = 200, h = 1)}.}

\item{type}{use raw or scaled density values?}
}
\value{
The input object with an added metadata column containing density
estimates.
}
\description{
Computes density estimates from a dimensional reduction embedding and adds the
density values to the object's metadata.
}
\examples{
\dontrun{
obj <- add_density_est(obj, reduction = "umap")
}
}
