% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core_cellular_machinery_genes.R
\name{core_cellular_machinery_genes}
\alias{core_cellular_machinery_genes}
\title{Core cellular machinery gene patterns and gene selection}
\usage{
core_cellular_machinery_genes(obj = NULL)
}
\arguments{
\item{obj}{Optional object containing gene features. If \code{NULL},
the pattern definitions are returned. Otherwise matching genes
are extracted from the object's feature set.}
}
\value{
If \code{obj = NULL}, a named list containing:
\itemize{
\item \code{cellular_machinery_patterns}
\item \code{core_cell_biology_patterns}
}

If \code{obj} is supplied, a character vector of unique genes matching
one or more cellular machinery patterns.
}
\description{
Returns curated regex patterns representing conserved cellular
machinery programs (e.g. ribosome, transcription, translation,
RNA processing, DNA replication, proteostasis, oxidative
phosphorylation, and cytoskeletal transport). Optionally extracts
matching genes from an object containing gene features.
}
\details{
The function provides two levels of annotation:
\itemize{
\item \code{cellular_machinery_patterns}: compact high-level categories
\item \code{core_cell_biology_patterns}: more granular biological processes
}

When \code{obj} is supplied, gene features are extracted using
\code{scexpr:::get_gene_features()} and all genes matching any
machinery pattern are returned.

Pattern categories include:
\itemize{
\item Ribosome (\code{RPL}, \code{RPS})
\item Translation initiation and elongation (\code{EIF}, \code{EEF})
\item Transcription (\code{POLR}, \code{TAF}, \code{GTF}, \code{MED})
\item RNA splicing and processing (\code{SRSF}, \code{HNRNP}, \code{SF3B}, \code{PRPF})
\item mRNA export (\code{NXF}, \code{THOC})
\item DNA replication (\code{MCM}, \code{RFC}, \code{RPA}, \code{ORC})
\item Chromatin regulation (\code{SMARC}, \code{CHD}, \code{HDAC})
\item Protein folding (\code{HSP}, \code{DNAJ})
\item Proteasome (\code{PSMA}, \code{PSMB}, \code{PSMC}, \code{PSMD})
\item Oxidative phosphorylation (\code{ATP5}, \code{NDUF}, \code{COX}, \code{UQCR})
\item Cytoskeleton and intracellular transport (\code{ACT}, \code{TUB}, \code{KIF})
\item Mitochondrial transcripts (\verb{MT-})
}

These signatures are intended for exploratory analysis of
housekeeping and cellular-state programs in single-cell and
bulk transcriptomic datasets.
}
\examples{
# Return pattern definitions
patterns <- core_cellular_machinery_genes()

# Extract all matching genes from a Seurat object
genes <- core_cellular_machinery_genes(seurat_obj)

# Access a specific pattern set
patterns[[1]]$ribosome
}
