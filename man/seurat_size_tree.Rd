% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seurat_size_tree.R
\name{seurat_size_tree}
\alias{seurat_size_tree}
\title{Print a size tree for a Seurat object}
\usage{
seurat_size_tree(object)
}
\arguments{
\item{object}{A Seurat object.}
}
\value{
The input \code{object}, invisibly. The function is called primarily for
its console output.
}
\description{
Prints the total memory size of a Seurat object and a tree-like summary of
its top-level slots using \code{\link[lobstr:obj_size]{lobstr::obj_size()}}.
}
\details{
The \code{assays} slot is expanded to show every assay and each assay's layers.
The \code{graphs}, \code{reductions}, and \code{misc} slots are expanded by one level to
show their named elements. Other slots, including \code{meta.data}, are shown
only at the top level.

Sizes are inclusive. For example, an assay's reported size includes its
layers. Because R objects can share memory, the sizes of child objects may
not sum exactly to the size of their parent.

The output hierarchy is:
\itemize{
\item Total Seurat object
\item All top-level slots
\item All assays
\item All layers within each assay
\item All graph, reduction, and \code{misc} elements
}

The function does not expand:
\itemize{
\item Assay slots other than layers
\item Graph or reduction internals
\item Columns of \code{meta.data}
\item Internals of \code{misc} elements
\item Other top-level slots
}
}
\examples{
\dontrun{
library(Seurat)
library(lobstr)

seurat_size_tree(pbmc_small)
}

}
