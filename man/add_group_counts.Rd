% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_group_counts.R
\name{add_group_counts}
\alias{add_group_counts}
\title{Add Distinct Sample Counts to Seurat Metadata}
\usage{
add_group_counts(
  obj,
  group_col,
  id_col = "orig.ident",
  output_col = paste0(group_col, "2")
)
}
\arguments{
\item{obj}{A Seurat object.}

\item{group_col}{A single character string naming the metadata column used
to define groups.}

\item{id_col}{A single character string naming the metadata column containing
identifiers to count. Defaults to \code{"orig.ident"}.}

\item{output_col}{A single character string naming the new formatted metadata
column. Defaults to \code{paste0(group_col, "2")}.}
}
\value{
The input Seurat object with \code{output_col} and \code{n} added to its
metadata.
}
\description{
Calculates the number of distinct identifiers within each level of a
metadata variable and adds a formatted label to the metadata of a Seurat
object. For example, a disease value of \code{"Control"} with five distinct
samples becomes \code{"Control (n = 5)"}.
}
\examples{
\dontrun{
sokid5 <- add_group_counts(
  obj = sokid5,
  group_col = "disease",
  id_col = "orig.ident",
  output_col = "disease2"
)
}
}
